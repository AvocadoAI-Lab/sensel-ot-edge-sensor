# Lab Modbus relay — enable with: docker compose --profile modbus-lab up -d
# Field deploy: set Address to relay IP and Port 502; disable modbus-simulator service.

deviceList:
  - name: relay-01
    profileName: ModbusRelay
    description: Lab Modbus relay (simulator)
    adminState: UNLOCKED
    operatingState: UP
    serviceName: device-modbus
    labels: [ot, modbus, relay, lab]
    protocols:
      modbus-tcp:
        Address: modbus-simulator
        Port: "1502"
        UnitID: "1"
        Timeout: "5"
        IdleTimeout: "5"
    autoEvents:
      - interval: 10s
        onChange: false
        sourceName: Status
