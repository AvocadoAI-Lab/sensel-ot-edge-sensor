# Local MQTT broker — feature summary bridge to EdgeX device-mqtt
listener 1883
allow_anonymous true
persistence false

# Feature summary topics (Packet Sensor publishes)
# sensel/ot/{sensor_id}/features/{window}

# EdgeX device-mqtt subscribes per device profile configuration
